# Railway configuration for AnuInv Sys
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check
healthcheckPath = "/api"
healthcheckTimeout = 100
healthcheckInterval = 10

